const express = require('express');
const app = express();

app.get('/user', (req, res) => {
  let id = req.query.id;

  // SQL Injection
  let query = "SELECT * FROM users WHERE id = " + id;

  res.send(query);
});

// Hardcoded Secret
const jwtSecret = "123456secret";

app.listen(3000);
